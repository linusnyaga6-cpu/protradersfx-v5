Plan reviewed and approved as a PLAN ONLY, not for execution.

Do not modify files, run `db push`, generate migrations, deploy, change Vercel variables, change Aiven permissions, or submit trades yet.

### Simplify the immediate repair

For the immediate production repair, do NOT add:

* a new schema-status endpoint
* `/health` schema reporting
* `/api/preflight` schema reporting
* new migration files
* changes to `protraders.ts`
* changes to the Deriv buy flow

The existing schema definition and trade logic should remain unchanged.

The only immediate database repair under consideration is provisioning the existing Drizzle schema once the production database target is positively identified.

### Current blocker

Vercel Production contains `DATABASE_URL`, but its value is hidden and the API integration returns 403.

Aiven PG Studio currently shows:

* `defaultdb`
* `avnadmin`
* PostgreSQL 18.6
* `pg_is_in_recovery() = false`
* `consumed_trade_proposals` does not exist
* zero application tables

This is strong evidence of an uninitialized Aiven database, but it is NOT yet proof that Vercel Production points to it.

### Therefore

STOP at this point.

Do not provision anything.

Do not make the application “work around” the missing table.

Do not expose or request the Vercel `DATABASE_URL`.

The next action is manual confirmation of the production database target using safe, non-secret information.

Once the target is confirmed, report it and STOP again for explicit approval before running:

`pnpm --filter @workspace/db run push`

If the target is confirmed as the empty Aiven database, the provisioning plan remains:

1. Aiven backup/snapshot.
2. Confirm target database.
3. Review schema diff.
4. Run the normal `db push`, never `push-force`.
5. Verify the resulting schema read-only.
6. Only then perform controlled application testing.

No real-money trading should be enabled or tested as part of schema provisioning.
