import { useState } from "react"
import { AlertTriangle, BrainCircuit, CircleHelp, Loader2 } from "lucide-react"
import { useListRecoveryIncidents, getListRecoveryIncidentsQueryKey, useAnalyzeRecoveryIncident, useGetAccount, getGetAccountQueryKey } from "@workspace/api-client-react"
import { Workspace } from "./markets"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { Badge } from "@/components/ui/badge"
import { AccountStrip } from "@/components/trading/account-strip"
export default function Recovery(){const q=useListRecoveryIncidents({query:{queryKey:getListRecoveryIncidentsQueryKey()}});const account=useGetAccount(undefined,{query:{queryKey:getGetAccountQueryKey(),refetchInterval:5000}});const analyze=useAnalyzeRecoveryIncident();const [result,setResult]=useState<any>(null);const list=Array.isArray((q.data as any)?.incidents)?(q.data as any).incidents:Array.isArray(q.data)?q.data:[];return <Workspace title="Recovery" eyebrow="Incident desk" description="Understand interruptions without turning advisory analysis into automation."><AccountStrip account={account.data} isLoading={account.isLoading} error={account.isError}/><div className="rounded-lg border border-amber-500/30 bg-amber-500/10 p-4 text-sm"><AlertTriangle className="mr-2 inline h-4 w-4 text-amber-700"/>AI analysis is advisory only. It cannot place, modify, or recommend an automatic trade.</div><div className="grid gap-5 lg:grid-cols-[1fr_360px]"><Card><CardHeader><CardTitle>Incidents</CardTitle></CardHeader><CardContent>{q.isLoading?<div className="h-24 animate-pulse rounded bg-muted"/>:list.length?<div className="space-y-3">{list.map((x:any,i:number)=><div key={x.id??i} className="flex items-center gap-3 rounded-lg border p-4"><CircleHelp className="h-5 w-5 text-amber-600"/><div className="flex-1"><div className="font-medium">{x.title??x.type??`Incident ${i+1}`}</div><div className="text-xs text-muted-foreground">{x.createdAt??x.created_at??"Time unavailable"}</div></div><Badge variant="outline">{x.status??"open"}</Badge><Button size="sm" variant="outline" onClick={()=>analyze.mutate({id:String(x.id)},{onSuccess:setResult})} data-testid={`button-analyze-incident-${x.id}`}>{analyze.isPending?<Loader2 className="animate-spin"/>:<BrainCircuit/>}Analyze</Button></div>)}</div>:<div className="p-8 text-center text-sm text-muted-foreground">No recovery incidents reported.</div>}</CardContent></Card><Card><CardHeader><CardTitle>Advisory analysis</CardTitle></CardHeader><CardContent>{result?<pre className="whitespace-pre-wrap text-sm leading-6">{JSON.stringify(result,null,2)}</pre>:<div className="text-sm text-muted-foreground">Select an incident to request a server-generated explanation. Recommendations remain for human review.</div>}</CardContent></Card></div></Workspace>}